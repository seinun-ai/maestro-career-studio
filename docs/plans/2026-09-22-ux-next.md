# UX next: no lost text, plain words, focus that stays — implementation plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or
> superpowers:subagent-driven-development) to implement this plan task-by-task. For this
> plan the owner chose parallel Grok 4.7 lanes (Cursor) with a full Claude review of each
> lane: see *Waves and lanes*.

## Goal Card

**Goal.** Close the older gaps that the follow-ups plan's goal critique and final browser
sweep found (its *Next plan* list). The app never loses typed text and never says "saved"
while something is pending. It never shows internal ids, edit paths or slugs. Keyboard focus
is never dropped to `<body>`, and selection, current state and contrast meet WCAG 2.2 AA
wherever they are measured.

**Principles**
- **Honesty about unsaved work outranks convenience.** If a gesture could lose typed text,
  it asks or keeps the text; the status line never says "saved" while something is pending.
- **Accessibility is not negotiable.** WCAG 2.2 AA: contrast pinned by computed tests, focus
  never dropped to `<body>`, programmatic state (`aria-current`, `aria-pressed`, `inert`).
- **Speak the user's language** in the web app, in chat and in MCP: no engine ids, no edit
  paths, no slugs.
- **No new dependencies.** Existing components, hooks and small shared helpers.
- **MCP and chat contracts are additive only.** Docstrings stay under the ~2,000-char budget.
- **Conventions change deliberately.** Each task updates `docs/frontend-conventions.md` in
  the same commit; SYSTEM.md changes are queued for the docs sweep (Task 20).

**Non-goals**
- Phase 2 of the studio direction (draft preview, three zones, job-fit chip), Phase 3
  (undo), remembering the sidebar across reloads, the extension side panel.
- The chat composer at 375px (owner: 768 only), the tabs/segments visual pass, and a
  "Save and leave" button.

**Autonomy: peer (adapt-and-advise).** Adapt *how* when a step conflicts with repo reality,
and log every deviation with one line of reason. Anything touching scope, another task's
interface or the Goal Card goes back to the planner. Never expand scope.

## How this plan is organised

Three appendices, each written read-only against `2cce6139` by a research agent, hold the
exact code. This file fixes the order, the owner decisions, the tests and the gates. Where an
appendix offers options, **Owner decisions** below is binding. Line numbers in the
appendices are at `2cce6139`; re-locate by the quoted code.

- `docs/plans/2026-09-22-ux-next-appendix-u-unsaved-work.md` (**U §n**): leaving editors,
  the gap page, dialogs, small unsaved-work gaps.
- `docs/plans/2026-09-22-ux-next-appendix-w-words-and-visuals.md` (**W §n**): edit words,
  résumé names, chip contrast, selection, tab focus.
- `docs/plans/2026-09-22-ux-next-appendix-f-focus-and-minor.md` (**F §n**): focus helpers,
  studio focus drops, retry focus, Cmd/Ctrl+S keystrokes, double submits, chat at 768.

## Owner decisions (2026-09-22, binding)

Asked of the owner:
1. **Back/Forward in an editor with unsaved edits asks** (U1 §Back/Forward option A: the
   sentinel history entry plus a capture-phase `popstate` listener). Its costs are accepted:
   the first edit drops forward history, and after a reload one Back press does nothing.
2. **Extra scope taken:** the focus drops found by reading (F1 §"Also found by reading", all
   of them) and the four route-level error screens with no retry (folded into F2); the
   double-submit guard on studio Save and every create/generate button (F4); a confirm before
   Regenerate replaces an existing cover letter; the leave prompt on Persona, Autofill,
   Prompts, the pasted job description on `/new`, and the Q&A cover-letter editor.
3. **Not taken:** the chat composer at 375px (F5 fixes 768 only; §11 item 31 keeps its chat
   clause).
4. **Execution:** Grok 4.7 lanes in Cursor, each fully reviewed, fixed and merged by Claude.
5. **"Submission uncertain" keeps the Needs-you orange** (both orange-800; recorded in the
   follow-ups plan).

Planner decisions (routine; each follows the appendix recommendation):
6. **Leave prompt:** "Leave without saving?" / "Changes you haven't saved on this page will be
   lost." / **Leave** (destructive) / **Stay** (initial focus). No "Save and leave". The chat
   composer does not register (it was not in the owner's list).
7. **One `GuardedLink`** wraps `next/link`, and a pin forbids importing `next/link` anywhere
   else (U1). The tailored studio registers `unsaved`, not `dirty`.
8. **Dialogs keep their drafts:** every close keeps the draft and the dismiss button reads
   **Close**; only New base résumé gets **Start over** (no confirm). "Ask for changes" stamps
   the basis it was made from and refuses to apply once the résumé changed (U3.2).
9. **Career KB textareas** (notes, point, inbox draft): Esc and Cancel both ask when the text
   changed (`useConfirmDiscard`, U4.2).
10. **Regenerate over an existing cover letter asks** "Replace your cover letter?" whenever a
    saved answer exists (no "was edited" signal is stored, so any saved text counts).
11. **Focus after a studio remount** (Load latest, Rebuild, foreign adoption) lands on
    `FullscreenEditorPage`'s `<main tabIndex={-1}>`; after ⋯ → "Edit raw JSON" it returns to
    the ⋯ button (F1).
12. **Edit words after a reload:** resolved chat cards describe edits at section level
    without entry names (W1); no server-stamped labels.
13. **Résumé names:** additive `base_resume_name` on application summary and detail responses
    (W2 option B); one `useBaseResumes()` replaces the four identical list queries;
    `humanizeSlug` only while loading or for an unknown slug.
14. **Template chips:** the picker shows no engine chip and no status badge (it lists only
    ready templates); the manage gallery and the editor show "LaTeX"/"Typst" and
    "Ready"/"Draft".
15. **Gap-target chips stay solid** (`bg-primary` + `aria-pressed`) as a recorded exception;
    their small-text contrast failures are fixed. Employment types and section presets move to
    tonal + Check + `aria-pressed`.
16. **Chip contrast reach:** the fix and the generalised pin also cover the Career KB entity
    chips and the three `text-amber-600` template badges.

---

## Before you start

- **Where to work:** each lane has its own worktree and branch (see *Waves and lanes*). Never
  `cd` to the main checkout `/Users/ajeyds/Projects/maestro-career-studio`: its `data/` is the
  owner's live database and its Docker stack on 3000/8001 is live.
- **Read** `SYSTEM.md`, `docs/frontend-conventions.md`, `frontend/AGENTS.md`, and before any
  Next API (`Link onNavigate`, router, `popstate`) `frontend/node_modules/next/dist/docs/`.
- **Python:** `/opt/anaconda3/bin/python3`, pytest and ruff from `<worktree>/backend`.
  **Frontend:** `node_modules` is installed in each lane worktree.
- **Lint has the React Compiler rules at error level.** Run `npm run lint` after every step.
- **Frontend duplication ceiling:** 505 lines / 42 clones at `2cce6139` (the committed
  baseline file still says 518/43; judge against the measured value). Get it with
  `slop_scan.py scan frontend --json` → `duplication`. Wave 2's ceiling is re-measured after
  wave 1 merges.
- **Backend `complexity_hotspots` is 424.** New tests must not raise it: split a test that
  reaches cc 10 instead of re-baselining.
- **Node tests are not in CI.** Pair every `lib/*.ts` behaviour with a pytest pin in
  `backend/tests/test_frontend_*.py`. `lib/*.ts` files import only relative paths or bare
  packages, and only as `import type` across lib files (F §0.7).
- **Mutation-check every pin you add**: break the code it guards, see exactly that pin fail,
  restore from a backup copy (never `git stash`).
- **SYSTEM.md is at 999/1000 and lanes don't edit it.** Queue SYSTEM.md changes (mostly §11
  removals) in your lane doc; Claude applies them in Task 20.
- **Commits:** one per task with the task's commit message. Lanes end each message with
  `Assisted-by: Grok 4.7 (Cursor CLI)`. Don't commit `docs/ux/`.
- **Baseline** at `2cce6139`: `pytest tests/ mcp_server/tests/ -q` 4582 passed, 2 skipped;
  node 86/86; tsc clean; lint 0 errors, 5 warnings.

---

## Waves and lanes

File overlap between these tasks is high, so the plan runs in two waves. Wave 1 builds the
shared pieces; wave 2 uses them. Lanes within a wave run in parallel and own disjoint file
sets (each lane doc lists them). Claude reviews each lane fully (adversarial code review with
mutation checks, an independent browser pass, gate reproduction), fixes what it finds, and
merges it into `claude/ux-next-plan`.

| Wave | Lane | Tasks | Theme |
|---|---|---|---|
| 1 | 1 `leave-guard` | 1, 2 | `GuardedLink`, the leave registry, Back/Forward |
| 1 | 2 `words` | 3, 4, 5, 6, 7 | edit words, résumé names, tab focus, chip contrast, selection |
| 1 | 3 `focus-core` | 8, 9, 10 | focus helpers, retry keeps focus, chat at 768 |
| 2 | 4 `autosave` | 11, 12, 13 | gap page, settings failure, leave prompt on other pages |
| 2 | 5 `drafts` | 14, 15, 16 | dialogs keep drafts, Q&A, KB editors |
| 2 | 6 `studio-focus` | 17, 18, 19 | studio focus drops, Cmd/Ctrl+S, single-flight |
| — | Claude | 20, 21 | docs sweep, verification and goal critique |

Known wave-1 overlap: Task 1 swaps the `next/link` import in 31 files, some of which lanes 2
and 3 also edit. Keep your edits away from the import block where you can; Claude resolves
the import-line conflicts at merge.

---

# Wave 1

## Lane 1 · Leave guard

### Task 1: Leaving through an in-app link asks

**Code:** U1 (§Fix design), decisions 6 and 7.

**Files:**
- new `frontend/lib/leave-guard.ts` (+ `.test.ts`), new `frontend/hooks/use-leave-guard.ts`
  (replaces `hooks/use-unsaved-changes-warning.ts`, which is deleted),
  new `frontend/components/guarded-link.tsx`, `frontend/app/providers.tsx`
  (`LeaveGuardListeners`, one app-wide `beforeunload`)
- the 31 files that import `next/link` (import swap only)
- registrations: `components/resume-editor/editor-body.tsx`,
  `components/resume-editor/tailored-resume-studio.tsx` (`unsaved`),
  `app/templates/[id]/page.tsx`
- new `backend/tests/test_frontend_leave_guard.py`
- `docs/frontend-conventions.md` (a leave-guard bullet; the *Tailored studio* sentence)

**Steps**
1. Pins (U1 §Tests): no file but `guarded-link.tsx` imports `next/link`; the three editors
   register; `beforeunload` is registered once; the old hook is gone. Node tests for the
   registry. They FAIL.
2. Implement the registry, `useLeaveGuard`, `GuardedLink` (cancel first, ask, replay through
   the router on Leave — `onNavigate` is synchronous, U §0.1), the listeners, the import swap
   and the three registrations.
3. Run pins, node, tsc, lint, `npm run build`, slop.
4. **Browser check** (U1 §Browser checks 1–6): dirty base studio → sidebar link asks; Stay
   keeps edits and focus; Leave navigates; clean studio navigates without asking; the tailored
   studio right after a Save doesn't ask; the template editor asks; reload/tab close still
   warns once (no double prompt after Leave).
5. Commit: `feat(ui): leaving an editor through a link asks first`.

### Task 2: Back and Forward ask too

**Code:** U1 §Back/Forward option A (decision 1).

**Files:** `frontend/lib/leave-guard.ts`, `frontend/hooks/use-leave-guard.ts`,
`frontend/app/providers.tsx`, `backend/tests/test_frontend_leave_guard.py`, conventions.

**Steps**
1. Pins for the sentinel entry and the capture-phase `popstate` listener. They FAIL.
2. Implement the sentinel (pushed on the first unsaved edit), the listener that asks, and the
   `router.replace` replay on Leave. Document the Next internal it depends on in a code
   comment and in the conventions bullet.
3. Run pins, node, tsc, lint, build.
4. **Browser check** (U1 §Browser checks 7–8), with real Back/Forward (Playwright
   `page.goBack()` and the keyboard shortcut): dirty studio → Back asks; Stay keeps the URL and
   the edits; Leave goes back one real entry; a clean studio's Back works in one press; after a
   Save the sentinel no longer traps Back.
5. Commit: `feat(ui): browser Back and Forward ask before leaving unsaved edits`.

## Lane 2 · Words and visuals

### Task 3: Edits are described in plain words

**Code:** W1, decision 12.

**Files:** new `frontend/lib/describe-edit.ts` (+ `.test.ts`), new
`frontend/components/edit-words-list.tsx`, `components/chat/edit-proposal-card.tsx`,
`components/resume-editor/instruct-sheet.tsx`, `components/resume-editor/editor-body.tsx`
(one prop), new `backend/tests/test_frontend_plain_words.py`, conventions ("An edit is
described, never printed").

**Steps**
1. Pins: the op-kind parity contract against `app.schemas.resume_edit.op_kinds()` (every kind
   has words), no `experience[`/op-name rendering in the two surfaces, both surfaces use
   `EditWordsList`. Node tests from W1. They FAIL.
2. Implement the describer (index shifting on a copy; never mutates the cached document), the
   list component, and the frozen words after Apply/Discard.
3. Run pins, node, tsc, lint, slop.
4. **Browser check:** a chat edit proposal and an Ask-for-changes proposal read as sentences
   ("Rewrite bullet 2 of Data Scientist at Acme"); after Apply the card keeps its words; after
   a reload it shows section-level words.
5. Commit: `fix(chat): edits read as plain sentences, never as paths`.

### Task 4: Résumés are called by their names

**Code:** W2 (names), decision 13.

**Files:** new `frontend/hooks/use-base-resume-label.ts` (+ `useBaseResumes()`),
`frontend/lib/types.ts`, the call sites W2 lists (chat cards, application panel, score panel,
proposals, tailor page, Applications Base column, `project-port-dialog.tsx`,
`app/base-resumes/page.tsx`, and the four list-query copies), backend
`app/schemas/application.py`, `app/routers/applications.py`, `docs/entities/application.md`,
pins in `test_frontend_plain_words.py` and one in `test_frontend_analytics.py`, backend tests
for the additive field.

**Steps**
1. Pins: no `humanizeSlug`/`baseResumeLabel` as a permanent name outside the fallback; the
   hook reads archived bases too; one `useBaseResumes()`; backend test that
   `base_resume_name` is present (and `None` for a deleted base, whatever W2 specifies). They
   FAIL.
2. Implement.
3. Run pins, node, tsc, lint, slop (frontend AND backend), the full backend suite (the field
   passes through MCP `list_applications`/`get_application`; the docstring budget ratchet
   must stay green).
4. **Browser check:** Applications Base column, chat cards, score panel and proposals show
   the résumé's display name; while the list loads a humanized fallback shows, never a blank.
5. Commit: `fix(ui): résumés are called by their names everywhere`.

### Task 5: Tab panels show focus

**Code:** W5 (and its Tailwind v4 `outline-none` trap).

**Files:** `frontend/components/ui/tabs.tsx`, one pin, conventions (focus; `TabsContent`).

**Steps:** pin (fails) → inset outline on `TabsContent` → gates → browser check (Tab into a
studio section panel shows a visible ring, light and dark, ≥ 3:1) → commit
`fix(ui): a focused tab panel shows its ring`.

### Task 6: Every tinted chip meets AA

**Code:** W3, decision 16.

**Files:** `frontend/components/status-chip.tsx`, `components/career/entity-card.tsx`,
`components/templates/requires-tex-badge.tsx` (and the other two amber-600 labels W3 names),
`backend/tests/test_frontend_color_roles.py` (one generalised computed pin replacing the
orange-only one), conventions (StatusChip).

**Steps:** the generalised pin fails on exactly the pairs W3 lists → move the shades →
gates → browser check (measure Interviewing, Accepted, Queued, Approved, Completed and the
template badges on page, card and muted, light and dark) → commit
`fix(ui): every tinted chip meets AA contrast`.

### Task 7: Selection looks and reads selected; the picker tells focus from choice

**Code:** W4 plus W2's engine chip; decisions 14 and 15.

**Files:** `settings/job-preferences-section.tsx`, `career/new-entity-dialog.tsx` (section
presets only), `gap-analysis/resolution-controls.tsx`, `templates/template-gallery.tsx`,
`templates/template-select.tsx`, `app/templates/[id]/page.tsx` (engine/status words),
pins in `test_frontend_color_roles.py`, conventions (colour roles, the picker).

**Steps:** pins (tonal + Check + `aria-pressed` on employment types and presets; the gap-chip
exception recorded; the picker's focus ring offset from the selected edge; no engine chip in
the picker) fail → implement → gates → browser check (light and dark; keyboard through the
picker: focused vs selected are distinguishable) → commit
`fix(ui): selection reads the same everywhere; focus and choice look different`.

## Lane 3 · Focus core

### Task 8: Focus and submit helpers

**Code:** F §"Shared helper" and F task split step 1.

**Files:** new `frontend/lib/query-state.ts` (+ `.test.ts`, with the parity case against
`unloadedLayer`), new `frontend/hooks/use-focus-return.ts`, `hooks/use-last-seen.ts`,
`hooks/use-single-flight.ts`, new `backend/tests/test_frontend_focus.py` (helper pins).

**Steps:** pins and node tests fail → implement the helpers (no caller changes yet, except
the Formatting panel's hand-rolled retry focus collapsing into the helper if F says so) →
node, tsc, lint → commit `feat(ui): shared helpers for focus return and single-flight submits`.

### Task 9: Try again keeps focus everywhere

**Code:** F2; decision 2 (the four route-level error screens).

**Files:** `frontend/components/load-error-state.tsx` and every caller F2 inventories (17,
plus the four route-level error branches: base and tailored studio routes, template editor,
tailor session); `components/resume-editor/formatting-panel.tsx` (simplify onto the helper);
`app/referrals/page.tsx` (the `.message` crash, the reordered branch); pins in
`test_frontend_query_error_states.py`, `test_frontend_studio.py`,
`test_frontend_referrals.py`, `test_frontend_focus.py`; conventions.

**Steps**
1. Pins: every caller uses `isLoadFailure`; the four reordered callers test failure before
   loading; the two 404-as-state callers remember the last error; the four route screens
   have a retry. They FAIL.
2. Implement.
3. Run pins, node, tsc, lint, build, slop.
4. **Browser check** (F2 §Browser checks): for five callers including Referrals, the health
   page and a studio route: block the endpoint, wait for the error, unblock, Enter on Try
   again → focus stays on the button while retrying, then lands in the recovered content,
   never `<body>`; no skeleton flash; Referrals doesn't crash.
5. Commit: `fix(ui): Try again keeps focus and every failed screen can retry`.

### Task 10: Chat fits at 768

**Code:** F5 option (a) only (decision 3).

**Files:** `frontend/components/chat/chat-page.tsx`, pins (new
`backend/tests/test_frontend_chat_layout.py` or `test_frontend_sidebar_nav.py`), conventions.

**Steps:** pin fails → container-gate the history rail → gates → browser check (768 with the
sidebar pinned and the rail open: no horizontal scroll, Send visible; 1280 unchanged) →
commit `fix(chat): the composer fits beside the history rail at 768`.

---

# Wave 2 (after wave 1 is merged)

## Lane 4 · Autosave and leave prompts

### Task 11: The gap page never says "Saved" early and never drops the last keystrokes

**Code:** U2. **Files:** `app/jobs/[id]/tailor/[sessionId]/page.tsx`, pins, conventions
("Two save models").
**Steps:** pins fail → `scheduleSave`, `editGen`, the unmount flush, `SaveIndicator` with
retry, two `useLeaveGuard` calls, `readOnly` while tailoring → gates → browser check (type
after a save: "Saving…" shows at once; leave within 800 ms: the edit is saved; a failed save
shows Try again and leaving asks) → commit
`fix(tailor): the gap page saves what you typed and says so honestly`.

### Task 12: Settings admit a failed autosave

**Code:** U4.3 (includes the MCP switch focus). **Files:** `lib/use-autosave.ts`,
`components/settings/autosave-status.tsx`, the settings sections that use it, pins,
conventions. **Steps:** pins fail → `failed` state + `retry`, "Not saved" + Try again,
`useLeaveGuard(failed)` → gates → browser check (force a 500: "Not saved" with Try again, no
check mark; leaving asks) → commit `fix(settings): a failed autosave says so and can retry`.

### Task 13: Other editing pages ask before leaving

**Code:** U §Owner decisions 2 (decision 2 here). **Files:** Persona, Autofill and Prompts
settings sections, `app/new/page.tsx` (the pasted JD; also its Extract button's single-flight
from Task 8's helper, F4 inventory), pins, conventions. **Steps:** pins fail → one
`useLeaveGuard` line per surface (+ `useSingleFlight` on Extract) → gates → browser check
(each page with unsaved text asks on a sidebar link; `/new` Extract double-click makes one
request) → commit `fix(ui): settings and the new-job page ask before dropping text`.

## Lane 5 · Drafts

### Task 14: Dialogs keep their drafts

**Code:** U3 (4a and 4b), decision 8; F4 single-flight for the create buttons in these
dialogs. **Files:** `components/base-resumes/new-base-resume-dialog.tsx` (+ the
getting-started caller), `components/career/new-entity-dialog.tsx` (keep presets code from
Task 7 intact), `components/resume-editor/instruct-sheet.tsx` (basis stamp; keep Task 3's
`EditWordsList`), `components/resume-health/demonstrate-skill-dialog.tsx`,
`components/career/send-to-resume-dialog.tsx`, `components/career/entity-detail.tsx`,
the `keepMounted` primitive change, pins (U3 §Tests), conventions (the dialog-draft bullet).
**Steps:** pins fail → implement → gates → browser check (U3 §Browser checks: Esc and overlay
keep text and LLM output; Close/Start over; a stale Ask-for-changes proposal refuses to apply;
double-click creates once) → commit `fix(ui): closing a dialog keeps what you typed`.

### Task 15: Q&A keeps the cover letter

**Code:** U4.1, decision 10, decision 2's Q&A leave prompt, F4 for its generate buttons.
**Files:** `components/qa-tab.tsx`, pins, conventions. **Steps:** pins fail → close only after
the save succeeds, show the new text at once; Regenerate over a saved answer asks; register
the editor with `useLeaveGuard`; single-flight generate → gates → browser check (a failed save
keeps the editor open with the text; Regenerate asks; leaving with an edit asks) → commit
`fix(qa): the cover letter survives a failed save and a regenerate asks`.

### Task 16: Career KB editors ask before discarding

**Code:** U4.2, decision 9; F4 for the capture box. **Files:** new `useConfirmDiscard`
(where U4.2 puts it), `components/career/notes-editor.tsx`, the point editor and inbox draft
editor U4.2 names, `components/career/capture-box.tsx` (single-flight), pins, conventions.
**Steps:** pins fail → implement → gates → browser check (Esc and Cancel on changed text ask;
unchanged text closes silently; capture double-click creates once) → commit
`fix(career): KB editors ask before throwing text away`.

## Lane 6 · Studio focus and submits

### Task 17: Studio focus never drops

**Code:** F1 including "Also found by reading"; decision 11. Diagnose "Edit raw JSON" first
(F1 §C).
**Files:** `components/resume-editor/editor-shell.tsx`,
`components/resume-editor/fullscreen-editor-page.tsx`, the
contact, summary and certification editors, `studio-overflow.tsx` and its overlays,
`components/confirm-dialog.tsx`, Rebuild's `returnFocus`, the raw pane's exits,
`EditableCard`, the chat rail toggles, the referral rows and the last-referral delete, pins in
`test_frontend_focus.py`, conventions.
**Steps:** pins fail → implement with `use-focus-return.ts` → gates → browser check (F1
§Browser checks, real keys, both studios, light and dark: every listed control keeps focus
somewhere visible) → commit `fix(studio): focus never falls to the page`.

### Task 18: Keys typed with Cmd/Ctrl+S are never lost

**Code:** F3. **Files:** `frontend/hooks/use-save-shortcut.ts`, `components/ui/chip-input.tsx`,
the section-rename and `EditableTitle` commit paths, pins in `test_frontend_studio.py`,
conventions (*Cmd/Ctrl+S*). **Steps:** pins fail → `flushSync` blur and synchronous refocus →
gates → browser check (real `keyboard.down("Meta")` + typing in the same instant: every
character lands; a chip draft still commits before the save) → commit
`fix(studio): keys typed with the save shortcut are never lost`.

### Task 19: One submit per click

**Code:** F4, decision 2. **Files:** `app/referrals/page.tsx`, `app/templates/page.tsx`
(create), the studios' Save (`studio-save-button.tsx` or its callers), pins, conventions.
**Steps:** pins fail → `useSingleFlight` at each site → gates → browser check (instant
double-click on each: one request) → commit `fix(ui): one click, one request`.

---

# Wrap-up (Claude)

### Task 20: Docs sweep

- Re-read every conventions bullet Tasks 1–19 touched against the code; fix contradictions
  between lanes.
- Apply every lane's *Queued for Task 20* list to SYSTEM.md: remove or narrow the §11 items
  these tasks closed (28 tab panels and FAB-ring as applicable, 29, 30, 32; 31 only its
  non-chat parts if any), stay at or under 1000 lines.
- In `docs/plans/2026-09-22-ux-followups.md` *Next plan*, replace the shipped items with one
  line pointing here.
- `python3 scripts/check_system_md.py`. Commit: `docs: conventions and SYSTEM.md match the next plan`.

### Task 21: Verification and goal critique

1. Gates: `pytest tests/ mcp_server/tests/ -q`; `ruff check .`; `node --test lib/*.test.ts`;
   `npx tsc --noEmit`, `npm run lint`, `npm run build`; slop on frontend, backend, extension
   (named); `check_system_md.py`. Record them below.
2. Browser regression sweep on the merged branch, light and dark, 1280/768/375: every
   browser check above plus the honest-studio and follow-ups features.
3. Goal critique against the Goal Card, not the plan. Default to approve.
4. Then superpowers:finishing-a-development-branch.

---

## Deviation log

| Task | Planned | Did instead | Why (Goal Card line) |
|---|---|---|---|
| Lane 2 (review) | As Grok 4.7 shipped Tasks 3–7 | Claude fix `46ec2b77`: tab-panel focus ring as an `after:` overlay above child cards; picker selection inside the card, focus outside; soft-deleted bases named on every surface; `/api/jobs/match` carries `base_resume_name`; monogram shades to -800; computed contrast pins for template labels, gap chips and monograms (decision 16 delivered) | Browser found cards covering the ring; reviewers found 18 surviving mutants and a backend hotspot rise |
| Lane 3 (review) + planner | A failed fetch shows `LoadErrorState` even over loaded data | Claude fix `91bdffea`: `isLoadFailure` is true only with NO data (a failed background refresh keeps loaded content; editors toast "Your edits on screen are kept"); 404 screens never flash the generic error on revisit; health report keeps focus on retry; retry chips; chat trigger truncates long names | Planner decision under "never lose typed text": a failed refresh swapped the base studio for an error and lost typed text |
| Lane 1 (review) | Grok 4.7's Back/Forward: a sentinel plus drift counting | Claude fixes `bf36c332`, `eb0511e2`: a pure, node-tested history machine (`lib/leave-guard.ts`); a patch under Next's `pushState`/`replaceState` stamps each entry's position relative to the previous one (holds past Chrome's 50-entry cap); fragment pops ignored; stale sentinels skipped; a stalled Leave falls back to `/` | Reviewers reproduced corrupted history (3 fast Backs, Back+Forward, skip link, save landing while asking, the 50-entry cap) — never mislead, never lose edits |

## Gate results

| When | Gate | Result |
|---|---|---|
| Baseline (`2cce6139`) | full backend / node / tsc / lint / duplication | 4582 passed, 2 skipped / 86 / clean / 0 errors, 5 warnings / 505 lines, 42 clones |
| Lane 1 merged (`3eea8963`) | full review ×2 | code review + browser pass, then a re-review of the rework; pins 305, node 130, tsc/lint clean, build OK, slop OK (505/42, hotspots 424), check_system_md 999/1000; 46/47 then 47/47 real-Chrome history scenarios incl. 52-entry cap |
| Lanes 2–3 merged (`8986aea0`, `47e7ce2b`) | full review each | code review + browser pass each on the integrated candidate; two import-line conflicts with lane 1 resolved |
| Wave 1 complete | full gates | pytest 4741 passed, 2 skipped; build OK; slop OK on frontend, backend, extension (frontend 481/40); check_system_md 999/1000 |
