# UX IA and copy: a clear Agent inbox, lists that hold their headers, Settings in tabs, plain words everywhere — implementation plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (this session) or
> superpowers:executing-plans (a parallel session) to implement this plan task-by-task. Each lane is
> implemented, then fully reviewed (code review with mutation checks, an independent browser pass,
> gate reproduction), fixed and merged into `claude/ux-ia-copy-plan` by Claude.

## Goal Card

**Goal.** A new user understands every screen on first read. The app says one plain word for each thing
on every surface (web app, Companion panel, and the server messages it shows). The Agent inbox is plainly
separate from the app's own features and names who proposed each job. Long lists keep their headers in
view and say when they are cut off. Settings and Profile are organised into tabs with one spacing rhythm.

**Principles**
- **Plain words over precise jargon.** No abbreviations (JD, KB), no internals (render, slug, endpoint,
  schema paths), no example text in blank fields, no "/" meaning "or". A term that must stay (ATS, MCP)
  is explained once, where it first appears.
- **One word per thing, everywhere.** The glossary (Appendix D0) is binding and pinned by a ratchet.
- **Honesty and safety nuance survive every cut.** Consent, "nothing is submitted without your yes",
  honesty warnings and "can't undo" are shortened, never dropped.
- **Everything from the last plan still holds.** Never lose typed text, never say "saved" while pending,
  focus never to `<body>`, WCAG 2.2 AA, one request per click.
- **No new dependencies. MCP and chat contracts are additive only** (docstrings ≤ ~2,000 chars).
- **Conventions change deliberately**, in the same commit as the code.

**Non-goals**
- Referrals / Contacts work (deferred by the owner), grouping or paginating the tracker, search and sort
  in the URL, an `X-Total-Count` header (Appendix B O3).
- Gender options and "restrictive covenant" wording (legal nuance; owner's call later).
- `keepMounted` on the job workspace tabs, a dot on a tab holding unsaved work.

**Autonomy: peer (adapt-and-advise).** Adapt *how* when a step conflicts with repo reality and log every
deviation with one line of reason. Scope, another task's interface or the Goal Card go back to the
planner. Never expand scope.

## How this plan is organised

Four appendices, each written read-only against `8cac7cf9`, hold the exact code, pins and browser checks.
This file fixes the order, the decisions, the lanes and the gates. Where an appendix offers options,
*Owner decisions* and *Planner decisions* below are binding. Line numbers in the appendices are at
`8cac7cf9`; re-locate by the quoted code.

- `docs/plans/2026-09-23-ux-ia-appendix-a-agent-inbox.md` (**A §n**): the inbox page, sidebar count, agent
  vocabulary, empty state, Connected agents card, "proposed by".
- `docs/plans/2026-09-23-ux-ia-appendix-b-sticky-and-cap.md` (**B §n**): sticky table headers and list
  toolbars, the "N most recent" notice.
- `docs/plans/2026-09-23-ux-ia-appendix-c-settings-profile.md` (**C §n**): Settings and Profile tabs, deep
  links, the leave guard across tabs, the shared tab row, status in the card header, the Models split,
  the spacing rhythm.
- `docs/plans/2026-09-23-ux-ia-appendix-d-copy.md` (**D §n**): the glossary, convention changes, the
  vocabulary ratchet, and every string change grouped by file.

Background: `docs/ux/research-sections-and-placement.md` and `docs/ux/copy-audit/part{1,2,3}-*.md`
(local-only, untracked).

## Owner decisions (2026-09-23, binding)

1. **Agent inbox** replaces "Agent proposals" (page title, sidebar); the URL stays `/proposals`.
2. **The inbox's funnel panel goes.** Only "Cap today N/M" stays, as a line in the page header. The funnel
   lives in Analytics › Overview only.
3. **The inbox toolbar matches Applications'** (one compact row, no stacked labels).
4. **A Needs-you count on the sidebar item**, in its accessible name, hidden at 0.
5. **One word per kind of agent:** **Assistant** (the in-app chat; the sidebar "Chat" item too; one
   conversation stays a "chat"), **Connected agents** (external MCP clients), **Companion** (the Chrome
   extension), **Suggested edits** (chat's edit cards). "Proposal" means only an agent-filed job.
6. **The empty inbox explains itself** and links to `docs/skills/README.md` (the `job-hunt` skill), the
   README's "Going all the way: agent applications" section, and Settings › Connected agents.
7. **"Proposed by"** on every proposal: the MCP client's name ("Proposed by Claude"), or "Queued by you"
   for the web app's Queue for agent. Additive backend field and migration.
8. **Sticky table headers and list toolbars app-wide**, as one shared mechanism and a conventions rule.
9. **A capped list says so at its end** ("Showing your 500 most recent …").
10. **Settings and Profile use top tabs.** Settings: **AI & models** (API keys, Models, Prompts) ·
    **Tailoring** (Quick tailor) · **Connected agents** (explainer, hints, Auto-apply) · **Appearance** ·
    **About**. Profile: **About you** (Persona, Market, Job preferences) · **Autofill**.
11. **Autosave status moves into the card header** (reverses the recorded `AutosaveRow` decision).
12. **The Models card splits** into Models, Model catalog, and Custom endpoint (under "Advanced").
13. **Copy:** Career KB → **Career history** (records are **items**, their lines are **bullets**); model roles
    **Fast, Smart, Assistant**; **Persona** keeps its name; **ATS score**, explained once per surface;
    **Add job / Save job** replaces "New application / Extract job"; inbox lanes **To review · Queued ·
    Applying · History**, chips **Proposed → Queued → Approved → Applied / Skipped** (funnel and Analytics
    too; Analytics' "In flight" → **In progress**); **resume** without accents; **no example text in blank
    fields**; **no "/" meaning "or"**.
14. **Copy scope includes** the Companion side panel and the server-written messages the UI shows.

## Planner decisions (routine; each follows the appendix recommendation unless noted)

15. **A&B overlap:** `lib/list-cap.ts` and `components/list-cap-notice.tsx` are built once, by Task 4 to
    **B §9's design**. A §7 adapts to it and passes the inbox's real `total` ("500 of 812").
16. **A open questions:** keep `/proposals` (A-Q1); no dot on the collapsed sidebar's reveal pill (Q2);
    SourceToggle's third segment reads **"Agents"** (Q3); the sidebar "Chat" item becomes **"Assistant"**
    (Q4, decision 5); score filter presets 50/60/70/80, no free number (Q5); the legacy Postgres revision
    is added (Q6); the job header pill keeps its status text, and "Proposed by …" is visible on the
    Overview card and inbox rows, with a hover and screen-reader by-line on the pill (Q7); client names
    map known clients and title-case unknown ones, never a slug (Q8); "Cap today" keeps its words, and its
    screen-reader text says "last 24 hours" (Q9); the count polls every 60 s (Q10); the KB timeline's
    "added by mcp" is fixed in Task 23 (Q11); Applications adopts `ListSearch` in Task 3 (Q12); "hunt"
    stays as the name of an agent's search run (Q13).
17. **B open questions:** headers stick only while the table fits (O1); "capped" wording at exactly 500
    (O2); no totals header (O3); **the user's own saved jobs are fetched separately from agent captures**
    so a busy hunt cannot push them out (O4, Task 4); no divider when stuck (O5); `ListToolbar` renders a
    `<search>` landmark (O6); the inbox bulk bar gets `scroll-padding-bottom` (O7, Task 13); the Career
    history draft inbox reuses `ListCapNotice` (O8, Task 4); Referrals is included, header only (O9).
18. **C open questions:** take the `lib/leave-guard.ts` pathname change so `?tab=` is written, and the
    `GuardedLink` same-page change (C-Q1); Connected agents order: explainer, then hints, then Auto-apply
    (Q2); every settings label uses the `Label` default size (Q3); a catalog row shows the model id as a
    second line only when it differs from the name (Q4); no dot on a tab with unsaved work (Q5); Custom
    endpoint starts expanded when it is in use (Q6); the tab row's scrollbar is hidden, and arrow keys
    scroll the focused tab into view (Q7); **Auto-apply and API keys register the leave guard** (Q8);
    `/profile` calls `useFocusSection()` once (Q9); the job page's tabs are unchanged, and the quirk is
    recorded (Q10).

19. **D wins on words.** Where appendix A or C proposes copy that differs from the D §0 glossary (A §5's
    "the Companion browser extension" and "Fast tailor", C's "Chat model", "postings", kept `e.g.`
    placeholders and "→" in backend messages, A's "Accepted." toast), waves 1–2 write the D §0 word, so
    wave 3 does not redo them.
20. **D open questions:** the inbox keeps its **Needs you** lane first (lanes: Needs you · To review ·
    Queued · Applying · History), and it is where the sidebar count points (D-Q1); Custom endpoint and Model
    catalog are titled **Custom AI server** and **Available models** (Q3); "Q&A" stays the tab name (Q4);
    deleting a chat asks first, like every other delete (Q5); health finding ids stay stable through a
    legacy key, so saved answers are never asked again (Q6, D §9.1); old version summaries and timeline
    text keep their words (Q7); "Companion" is a proper name in labels ("Open Companion") and "the
    Companion" in sentences (Q8). `UBIQUITOUS_LANGUAGE.md` gains an "On screen" column (D §11) so code
    terms and screen words are both recorded.

---

## Before you start

- **Where to work:** each lane has its own worktree and branch, from `claude/ux-ia-copy-plan` at the
  commit that adds its lane doc. Never `cd` to the main checkout `/Users/ajeyds/Projects/maestro-career-studio`:
  its `data/` is the owner's live database and its Docker stack on 3000/8001 is live.
- **Read** `SYSTEM.md`, `docs/frontend-conventions.md`, `frontend/AGENTS.md`, your appendix sections, and
  `frontend/node_modules/next/dist/docs/` before any Next API.
- **Python:** `/opt/anaconda3/bin/python3`; pytest and ruff from `<worktree>/backend`. **Frontend:** run
  `npm ci` in a fresh worktree.
- **Lint has the React Compiler rules at error level.** `npm run lint` after every step.
- **Pins:** `backend/tests/test_frontend_*.py` source-reading tests. `lib/*.ts` behaviour gets a node test
  AND a pytest pin (node tests are not in CI). Mutation-check every pin you add (break the code, see
  exactly that pin fail, restore from a backup copy, never `git stash`).
- **Frontend duplication:** measure on a CLEAN `git archive HEAD` export
  (`slop_scan.py scan <export>/frontend --json` → `duplication`), never after a build. Ceiling = the value
  at the branch point (recorded in the gate table at Task 0); hold your delta at zero or below.
- **Backend `complexity_hotspots` ≤ 424** (423 at the last plan's end); split a test that reaches cc 10.
- **SYSTEM.md sits at 1000/1000. Lanes don't edit it.** Queue changes in the lane doc; Claude applies
  them in Task 24.
- **Commits:** one per task with the task's message, ending
  `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` (or the executor's own attribution line).
  Never commit `docs/ux/`.
- **Browser checks:** a throwaway stack per lane (SYSTEM.md §9): uvicorn on the lane's port with its own
  SQLite file and `*_DIR`s under a scratch dir, `API_PROXY_BACKEND=… npx next dev -p <port>`, made-up
  seed data, Python Playwright (`/opt/anaconda3/bin/python3`, `channel="chrome"`), real keys and
  pointer, light and dark, 1280/768/375.

---

## Waves and lanes

| Wave | Lane | Tasks | Theme | Appendix |
|---|---|---|---|---|
| 0 | Claude | 0 | branch, baselines, lane docs | — |
| 1 | 1 `proposed-by` | 1 | backend field, migrations, MCP | A §9 |
| 1 | 2 `lists` | 2, 3, 4, 5 | sticky primitives, Applications, Referrals, cap notice | B |
| 1 | 3 `settings-tabs` | 6, 7, 8, 9 | tabs, deep links, leave guard, tab row | C §1–5 |
| 1 | 4 `settings-cards` | 10, 11, 12 | header status, Models split, spacing | C §6–8 |
| 2 | 5 `inbox` | 13, 14 | the inbox page, sidebar count | A §1–4, §6, §7, §9 (UI) |
| 2 | 6 `agent-words` | 15 | one word per kind, Connected agents card, tracker by-line | A §5, §8, §9 (tracker) |
| 3 | Claude | 16 | glossary, conventions, the vocabulary and error-words ratchets | D §0, §1 |
| 3 | 7 `copy-jobs` | 17, 18 | jobs and tracking, gap page | D §2, §3 |
| 3 | 8 `copy-resumes` | 19, 20 | resumes, studios, health, templates; Career history | D §4, §5 |
| 3 | 9 `copy-settings` | 21 | Settings and Profile cards; Assistant, shell, errors, setup | D §6, §7 |
| 3 | 10 `copy-companion-server` | 22, 23 | Companion panel; server-written messages | D §8, §9 |
| — | Claude | 24, 25 | docs sweep; verification, goal critique, finish | — |

Wave 1 lanes own disjoint files (each appendix's ownership list). Wave 2 starts after wave 1 is merged
(the inbox needs Task 4's `ListCapNotice`, Task 2's `ListToolbar` and Task 1's types; the vocabulary lane
touches Settings files that lanes 3–4 restructure). Wave 3 starts after wave 2 is merged, because copy
touches nearly every file. Task 16 lands first in wave 3 (it adds the ratchet the other copy lanes shrink).

---

# Wave 0

### Task 0: Branch, baselines, lane docs (Claude)

1. Branch `claude/ux-ia-copy-plan` from `main` (`8cac7cf9`); commit this plan and the four appendices.
2. Record baselines in *Gate results*: full backend suite, node tests, tsc, lint, build, frontend
   duplication on a clean export, backend hotspots, `check_system_md.py`.
3. Write one handoff doc per wave-1 lane (`docs/plans/2026-09-23-ux-ia-lane<N>-<name>.md`) in the format
   of `docs/plans/2026-09-22-ux-next-lane5-drafts-cursor-grok47.md` (Goal Card, ground truth, tasks, scope,
   environment with the lane's ports, gates, escalation, logs). Commit, then create each lane's worktree
   and branch at that commit.

---

# Wave 1

## Lane 1 · Proposed by

### Task 1: Every proposal records who filed it

**Code:** A §9 (backend and MCP half), decision 7, planner decision 16 (Q6, Q8).
**Files:** A's T-A1 ownership list (model, schemas, `routers/proposals.py`, `routers/jobs.py`,
`services/proposals.py`, the SQLite migration and the legacy Postgres revision, `mcp_server/server.py` and
`client.py` (propose only), backend and MCP tests, `docs/entities/others.md`).
**Steps**
1. Tests first (A §9): the column exists and round-trips; list and detail expose it; a web "Queue for
   agent" stamps `you`; an MCP propose carries the client name through the header plumbing; an agent can
   never file as `you`; past Queue-for-agent rows backfill to `you`; the legacy import stays strict. They FAIL.
2. Implement. Generate revision ids with `uuid.uuid4().hex[:12]`.
3. Run the new tests, `pytest tests/ mcp_server/tests/ -q`, ruff, slop (backend), the MCP docstring
   budget ratchet.
4. Commit: `feat(proposals): every proposal records who filed it`.

## Lane 2 · Lists

### Task 2: Sticky table headers and list toolbars

**Code:** B §2–5, decision 8, planner decision 17 (O1, O5, O6).
**Files:** `components/ui/table.tsx`, `components/empty-state.tsx` (`TableFrame` only), new
`components/list-toolbar.tsx`, `app/globals.css`, new `backend/tests/test_frontend_sticky_lists.py`.
**Steps:** pins fail → `Table` `minWidth` + `stickyHeader`, `TableFrame` `overflow-clip`, `ListToolbar`
(a `<search>` landmark) with the `tall:` variant, `scroll-padding-top` → node, tsc, lint, build → browser
check (B §12 rows 1–7, incl. WebKit if available; else record "Chromium only" and apply the B §5
contingency if focus is obscured) → commit `feat(ui): list headers and toolbars stay in view`.

### Task 3: Applications and Referrals adopt them

**Code:** B §6, §7; planner decision 16 (Q12: `ListSearch` from A §3 is created here and reused by Task 13).
**Files:** `app/applications/page.tsx` (toolbar, `<Table>`, `ListSearch`), `app/referrals/page.tsx`
(the `<Table>` line), new `components/list-search.tsx`, pins appended to `test_frontend_sticky_lists.py`,
`test_frontend_placeholders.py` (`_PROMPTS` row moves to `list-search.tsx`).
**Steps:** pins fail → adopt → gates → browser check (scroll a 60-row tracker at 1280/768/375 light and
dark; Tab through rows never hides under the header; row menus open above it) → commit
`feat(ui): the tracker and referrals keep their headers in view`.

### Task 4: A capped list says so

**Code:** B §9, §10; planner decisions 15, 17 (O2, O4, O8).
**Files:** new `lib/list-cap.ts` (+ `.test.ts`), new `components/list-cap-notice.tsx`,
`app/applications/page.tsx` (queries: the user's saved jobs fetched separately from agent captures — check
the jobs router's `source` filter; add an additive one with tests if it doesn't exist), the Career history
draft inbox (`components/career/inbox-panel.tsx`, one line), pins.
**Steps:** pins and node tests fail → implement → gates → browser check (seed 500+ rows: the notice shows
at the end with filters on and off; below 500 it doesn't; a busy agent hunt no longer hides user-saved
jobs) → commit `feat(ui): a list cut off at 500 says so`.

### Task 5: Conventions for lists

**Code:** B §11. **Files:** `docs/frontend-conventions.md` (the list bullets); queue §11 item 5's reword
for Task 24. Commit `docs: list headers stay in view and a capped list says so`.

## Lane 3 · Settings tabs

### Task 6: Settings and Profile open in tabs

**Code:** C §1, §2; decision 10; planner decision 18 (Q2, Q7, Q9).
**Files:** new `lib/settings-tabs.ts` (+ `.test.ts`), new `components/settings/settings-tabs.tsx`,
`app/settings/page.tsx`, `app/profile/page.tsx` (leave a mount point for Task 15's Connected agents card),
new `backend/tests/test_frontend_settings_pages.py`.
**Steps:** pins and node tests fail → tab table, `keepMounted` panels, `?tab=` via `history.replaceState` →
gates → browser check (each tab by mouse and keyboard; unsaved Persona text survives a tab switch; reload
keeps the tab) → commit `feat(settings): Settings and Profile open in tabs`.

### Task 7: Every deep link lands on its tab

**Code:** C §3. **Files:** `lib/use-focus-section.ts`, the new `anchorHref()` helper, every link in C §3's
inventory, pins. **Steps:** pins fail → implement → gates → browser check (every inventoried link, and an
old hash-only link, opens the right tab and rings the card) → commit
`fix(settings): every link into Settings and Profile opens the right tab`.

### Task 8: The leave guard and focus hold across tabs

**Code:** C §4; planner decision 18 (Q1). **Files:** `lib/leave-guard.ts` (pathname compare),
`components/guarded-link.tsx` (same-page links don't ask), `lib/focus.ts` (`focusIfStranded`), pins in
`test_frontend_leave_guard.py` and node tests. **Steps:** the new history tests fail (2/5) → implement →
all history tests pass → browser check (Back/Forward after a tab switch with unsaved Persona text asks
once; after a Save Back works in one press; a link to another tab of the same page doesn't ask) → commit
`fix(ui): the leave guard ignores tab switches on the same page`.

### Task 9: The shared tab row scrolls inside itself

**Code:** C §5. **Files:** `components/ui/tabs.tsx`, pins. **Steps:** pin fails → implement → browser
check at 375/768/1280 (job page, New base resume dialog, both settings pages: no page overflow, arrow keys
bring the focused tab into view) → commit `fix(ui): a long tab row scrolls instead of widening the page`.
Queue the two §11 item 31 clauses this closes.

## Lane 4 · Settings cards

### Task 10: Autosave status sits in the card header

**Code:** C §6; decision 11. **Files:** `components/settings/setting-card.tsx`, `autosave-status.tsx`, each
card's status placement, pins (`test_frontend_settings_autosave.py` and new ones), conventions (the
`AutosaveRow` record is rewritten). **Steps:** pins fail → the header portal slot → gates → browser check
(force a 500: "Not saved" + Try again in the header; no empty row in Market or Job preferences; the
description doesn't re-wrap on save) → commit `fix(settings): the save status sits beside the card title`.

### Task 11: The Models card splits in three

**Code:** C §7; decision 12; planner decision 18 (Q4, Q6). **Files:** `models-section.tsx`,
`model-catalog-panel.tsx`, `llm-endpoint.tsx`, new `lib/model-catalog.ts` (+ `.test.ts`), pins. Includes
provider names ("OpenAI", "Gemini", "Built-in"), quiet remove buttons with names, focus kept while Test,
Sync and Remove run. **Steps:** pins fail → implement → gates → browser check → commit
`fix(settings): models, catalog and custom server are three plain cards`.

### Task 12: One spacing rhythm, and every explicit Save asks before leaving

**Code:** C §8; planner decision 18 (Q3, Q8). **Files:** every settings and profile card C §8 lists, pins.
**Steps:** pins fail → apply the rhythm; `useLeaveGuard` on Auto-apply and API keys; the Advanced prompts
toggle keeps typed text and gets `aria-expanded` → gates → browser screenshots at 1280/768/375 light and
dark → commit `fix(settings): every card uses one spacing rhythm`.

---

# Wave 2 (after wave 1 is merged)

## Lane 5 · Inbox

### Task 13: The Agent inbox page

**Code:** A §1, §2, §3, §6, §7, §9 (inbox, Overview card, job header); decisions 1–3, 6, 7; planner
decisions 15–17 (Q5, Q7, Q9, O7). **Files:** A's T-A2 ownership list; reuses `ListToolbar`, `ListSearch`,
`ListCapNotice`. Deletes `components/proposals/funnel-strip.tsx`.
**Steps:** pins fail → implement → gates → browser check (A §1–3, §6, §7 checks; the empty state's links
resolve on GitHub once `main` is pushed — until then, check the anchors against the local README) → commit
`feat(proposals): the Agent inbox names who proposed each job and keeps only what you act on`.

### Task 14: A Needs-you count in the sidebar

**Code:** A §4; decision 4; planner decision 16 (Q2, Q10). **Files:** A's T-A3 ownership list. **Steps:**
pins fail → implement → gates → browser check (count appears, hides at 0, read in the link's name, AA on
all four row states) → commit `feat(ui): the sidebar says how many proposals need you`.

## Lane 6 · Agent words

### Task 15: One word per kind of agent, and the Connected agents tab

**Code:** A §5, §8, and A §9's tracker mark; decision 5; planner decisions 16 (Q3, Q4, Q13), 18 (Q2).
**Files:** A's T-A4 ownership list, `app/applications/page.tsx` (A §5 rows and the tracker by-line),
`components/app-sidebar.tsx` (the Assistant item, after Task 14), the Connected agents mount in
`app/settings/page.tsx`. **Steps:** pins fail (`test_frontend_agent_words.py`) → implement → gates →
browser check → commit `fix(ui): one word for each kind of agent`.

---

# Wave 3 (after wave 2 is merged)

Every copy task follows D §0–1 (glossary, conventions, ratchet) and its D group's table. Each task: update
the pins that read the strings it changes (D lists them) → change the strings → shrink the ratchet's
per-file allowlist for the files it finished → gates (incl. `test_frontend_*.py`, full backend suite for
Tasks 22–23) → browser spot-checks from its D group, light and dark, 1280 and 375.

### Task 16: The glossary, the rules and the ratchet (lands first in wave 3)

**Code:** D §0, §1; decision 13 (placeholders, slash, "(optional)", "resume"). **Files:**
`docs/frontend-conventions.md` (Microcopy rules, glossary bullet, Settings vs Profile rule),
`backend/tests/test_frontend_placeholders.py` (refuses `e.g.` and sample text; pending counts per lane),
new `test_frontend_vocabulary.py` and the error-words test (per-lane pending counts that only go down),
new `lib/error-text.ts` (+ `.test.ts`), the `lib/api.ts` unreachable-server message,
`components/ui/label.tsx` ("(optional)"). Claude lands this before the wave-3 lanes branch.
Commit `docs(copy): one glossary, and a ratchet that keeps it`.

### Tasks 17–23: The copy changes, by group

| Task | Lane | D group | Pending at Task 16: words / raw errors / placeholders | Commit |
|---|---|---|---|---|
| 17 | 7 | D §2 jobs and tracking (tracker, job workspace, `/new`, Analytics, referrals) | 95 in 23 files / 45 in 18 / 10 in 8 rows | `fix(copy): jobs and tracking speak plainly` |
| 18 | 7 | D §3 the gap page | 47 in 3 / 6 in 1 / 5 in 4 | `fix(copy): the gap page speaks plainly` |
| 19 | 8 | D §4 resumes, studios, health, templates | 128 in 30 / 46 in 18 / 17 in 17 | `fix(copy): resumes, health and templates speak plainly` |
| 20 | 8 | D §5 Career history | 85 in 14 / 32 in 15 / 17 in 17 | `fix(copy): the Career KB becomes Career history` |
| 21 | 9 | D §6 Settings and Profile cards; D §7 Assistant, shell, errors, setup | 50 in 17 / 30 in 18 / 15 in 15 | `fix(copy): settings, the Assistant and errors speak plainly` |
| 22 | 10 | D §8 the Companion panel | 33 in 12 / — / — | `fix(companion): the side panel speaks plainly` |
| 23 | 10 | D §9 server-written messages (keep MCP meaning; D §9.1's stable finding ids) | — (the ratchets read the frontend and the panel) | `fix(api): messages the app shows speak plainly` |

**Each task's pending counts live in the ratchet tests, one block per task:** `_PENDING_T<n>` in
`backend/tests/test_frontend_vocabulary.py` (banned words per file) and
`backend/tests/test_frontend_error_words.py` (raw `err.message` sites per file), and
`_PENDING_EXAMPLES_T<n>` in `backend/tests/test_frontend_placeholders.py` (each example placeholder and how
often it appears). A count may only go down: a task lowers its rows as it sweeps (the tests fail until it
does) and deletes its block when it lands, which is its definition of done. A new violation in a file with
no pending row fails at once. The numbers are at Task 16 (`python tests/test_frontend_vocabulary.py`
and `python tests/test_frontend_error_words.py` from `backend/` print the live counts). Where D's pending
lists differ, the tests win: D was measured at `8cac7cf9`, before waves 1–2.
The real bugs the audit found in copy (D lists each with its pin) are fixed inside their group's task.
The last copy task to merge deletes the `_PENDING` dicts.

---

# Wrap-up (Claude)

### Task 24: Docs sweep

- Re-read every conventions bullet Tasks 1–23 touched against the merged code; fix contradictions.
- Apply every lane doc's *Queued for Task 24* list to SYSTEM.md (§11 items 5, 30, 31, others as closed),
  the vocabulary in §5 and §7 (Agent inbox, Connected agents, Companion, Career history), and stay at or
  under 1000 lines (groom first; the cap moves only after a grooming pass fails).
- README and `docs/GETTING_STARTED.md`: the renamed screens and words (Career history, Agent inbox,
  Assistant, Add job).
- `python3 scripts/check_system_md.py`. Commit `docs: SYSTEM.md and the guides speak the new words`.

### Task 25: Verification and goal critique

1. Gates: `pytest tests/ mcp_server/tests/ -q`; `ruff check .`; `node --test lib/*.test.ts`; `npx tsc --noEmit`,
   `npm run lint`, `npm run build`; slop on frontend, backend, extension (named); `check_system_md.py`.
2. Browser regression sweep on the merged branch, light and dark, 1280/768/375: every browser check above,
   plus the last plan's (ux-next) checks on the screens this plan touched.
3. A first-read copy pass: a reviewer who has not seen the audits reads every screen and the Companion
   panel as a new user and lists anything still unclear.
4. Goal critique against the Goal Card, not the plan. Default to approve.
5. Then superpowers:finishing-a-development-branch.

---

## Deviation log

| Task | Planned | Did instead | Why (Goal Card line) |
|---|---|---|---|
| Wave 2 (ownership) | Task 15 edits the tracker page and the sidebar's Assistant item | Lane 5 owns `app/applications/page.tsx` (A §5 rows, the by-line, `keepPreviousData`) and `components/app-sidebar.tsx` (count, labels, Assistant); lane 6 keeps the rest of A §5 and the Connected agents card | The by-line needs lane 5's `lib/agent-name.ts`, and both tasks edited the sidebar: parallel lanes must own disjoint files |
| 15 (plan defect, A §8) | The Connected agents card's can/can't lists as A §8 wrote them (agents can't "delete your career record"; they only save, score, tailor, read and propose) | Lane 6 rewrote both lists against the MCP tools (`backend/mcp_server/server.py`): agents also change career history without a draft step (`kb_create_entity`, `kb_edit_entity`, `kb_edit_profile`), create and edit resumes, and submit after your yes; they can't delete an item or a bullet. The card's doc comment names the tool behind each line | Never imply the app hunts or applies, and never under- or overclaim what an agent can do: A §8 misled a new user |
| 15 (plan defect, A §5 row 45) | Autofill coverage's Clear confirm: "Capture stays on. Turn it off in the Companion's ⋯ menu." | "… It cannot be undone. Clearing removes what's recorded so far. Capture continues while the Companion runs." The Companion has no ⋯ menu and no capture switch (`extension/README.md`, "Turn it off"). **Note for Task 17/21:** Appendix D's later rewrite of this confirm (`autofill-coverage-card.tsx:98-101`) must drop its "Turn it off in Companion's ⋯ menu." sentence too; `test_frontend_agent_words.py` refuses "⋯ menu" in that file | Every sentence tells the truth about the product |
| 13 (scope) | Tracker and inbox status words ("Queue", "Applied") are Task 17's (D §2, wave 3) | Lane 5 pulled them into its files (Queue for Accept on rows, bulk bar and job header; toasts say Queued) and changed the shared chip vocabulary in `components/status-chip.tsx` (`PROPOSAL_STATUS_CHIP`: `submitted` → "Applied", `submission_uncertain` → "Check if sent", D2.1 rows :133/:134), so the tracker's saved-job chip and the job pill changed with it. Task 17 finds those rows done; the pipeline card's cap line (D2.7) stays Task 17's | One word per thing: a second vocabulary in the inbox would have split them (planner pulled the words forward) |
| Wave 2 merge | Each lane's own link constants and heading pins | `lib/agent-links.ts` is the one home for `REPO` and the three guide URLs (it gains `CONNECT_AGENT_GUIDE_URL`); the card imports them. One heading pin (`test_frontend_agent_inbox.py::test_the_empty_state_links_point_at_real_headings`, all three anchors) and one home pin (`test_frontend_agent_words.py::test_the_agent_links_have_one_home`: one definition of each constant across `app/`, `components/`, `hooks/`, `lib/`; `REPO` matches CITATION.cff) | Logic in two places goes through one helper |
| 16 (ratchet blocks) | D's pending blocks per appendix lane (L0–L8), counted at `8cac7cf9` | One block per wave-3 task (`_PENDING_T17`…`_PENDING_T22`), re-measured at `9e2222f3`; the placeholder blocks count each example's occurrences (a copied example fails) instead of holding a set | Each task knows exactly what it must drive to zero, and the counts match the tree the lanes branch from |
| 16 (error-words scan) | D §1.3's one regex (133 sites at `8cac7cf9`) | A sink scan: a toast's or a load error's whole argument, plus JSX children (`{query.error.message}`) and helpers that hand a message on; 159 sites. D's regex missed `err instanceof ApiError ? err.message` (5 health toasts), template-literal toasts and every JSX child. `app/error.tsx` (development only), `lib/api.ts` (the browser's own fetch error) and `lib/error-text.ts` (the one reader) are allowlisted with reasons | A new user never meets raw server text; the ratchet has to see it to shrink it |
| 16 (proxy words) | `lib/api.ts` rewrites the can't-reach-the-server message | Also `app/api/[...path]/route.ts`: its 502 `detail` was "Upstream fetch failed (<backend URL>): <error>", which `apiFetch` shows as is on the normal same-origin path. It now says the same desktop sentence and logs the detail on the server (pinned in `test_frontend_error_words.py`) | Plain words everywhere, including the one error every desktop user meets first |
| 16 (conventions scope) | D §1.1 a–i | a, b, c, i done, plus the Settings vs Profile rule by tab. d (autosave "Not saved"), e (studio empty preview), f (sidebar), g (Add job naming) and h (setup guidance) quote strings wave 3 has not changed yet: each moves to the task that changes the string (d → 18, e → 19, f and g → 17, 20, 21, h → 17 and 21). The glossary bullet already binds the words | The doc describes the code as it is; conventions change in the same commit as the code |
| 16 (label) | D §1.2 swaps "· optional" for "(optional)" in the same trailing span | The text and the marker render as one inline run inside the flex label, so a wrapped label keeps "(optional)" after its last word (lane 4 found the old suffix at the far right of wrapped Autofill labels) | WCAG and first-read clarity on narrow cards |
| Wave 3 merge (ratchets) | "The last copy task to merge deletes the `_PENDING` dicts" | Each merge kept only the pending blocks present on both sides; after lane 10 none was left, so `c1697a8e` deleted `_BLOCKS`/`_PENDING` (vocabulary, error words) and `_EXAMPLE_BLOCKS`/`_PENDING_EXAMPLES` (placeholders) and their shrink/owner tests: every file is judged with no exemption but `_ALLOWED` | One word per thing, with no file excused |
| Wave 3 merge (lane 9 deferred, models-section "..") | Fix a "…was not tested: ${why}. Check…" double stop at `models-section.tsx:~130` | Lane 9's fix pass (`b27f6821`) had already rewritten it (`unreachableText`, `keyProblemIn`); the ".." that lane 10's full-sentence messages DO cause was in `schemaIssuesWords` (a sentence schema message such as `TITLE_COLLISION_MESSAGE` + ". Fix it"), fixed and pinned in `describe-edit.test.ts` | Every sentence reads as one |
| Wave 3 merge (import words, planner decision) | Career history's "Add files" (lane 8) and the Score tab's "Import resumes" (lane 7) | Both open the upload dialog, so both say its title, **Import resumes and documents**; the first-run card keeps **Import resumes** (it opens the resume-only dialog). Glossary updated | One word per thing |
| 24 (glossary) | Re-read the conventions bullets the plan touched | Folded the Naming bullet's agent words, Add job and inbox lanes into *Canonical terms* (one glossary; Naming points to it) and corrected every claim the merged code contradicted (the ATS warning line, Copy ID for connected agents, Write a new version, Update PDF, Queue in Agent inbox, `KB_DRAFTS_LIMIT`, `ListSearch`'s file, Checks / Must fix / Serious, Import resumes vs Import resumes and documents, Suggested edits' surfaces, Accepted as an application status). Two code strings still break the ATS "our estimate" rule (`tailored-resume-studio.tsx:879-881`, `auto-apply-section.tsx:68`, both say an ATS "rates") and Auto-apply's hints say bare "Agents" (`:50`, `:75`): left for Task 25, since this task edits docs only | Every sentence tells the truth about the product |
| 24 (planner decision, role titles) | Allowlist role titles in `_ALLOWED` if the ratchet flags them | No test edit: the slash rule's own pattern already exempts AI/ML and UI/UX (`test_frontend_vocabulary.py`), and "AI/ML Engineer" reaches the screen only as catalog data, which the ratchet does not scan. The conventions' slash rule now names the exception | One word per thing, with deliberate exceptions written down |
| 24 (§12 vs §13) | A §12 lesson "a rollback needs the pg_dump restored", and lane 10's §12 lesson on `/choose` | The rollback note is a §13 row note on `postgres-to-sqlite` (it dies with the row); the `/choose` lesson is carried by the rewritten §6 invariant (a §12 copy would say it twice). `LLMProviderError`'s lesson is integrated into the existing §12 bullet | Integrate, don't append |
| 25 (fix 1, Needs you) | "Keep it" moves `needs_decision` back to To review | The PATCH runs `record_decision`, which must link an application: the row sends none (the server links the proposal's, or its `chosen_base`'s), the job page sends the job's application when the proposal has none (read from the proposal detail). With nothing to link, the 409 is now a sentence ("This job has no tailored resume to link yet. Tailor one first, then try again."), which agents read too (still a 409). `needs_human` gets no transition: the row and the Overview card say the agent stopped and is waiting in its own chat, in the agent's words | Every sentence tells the truth about the product; never offer a transition that cannot succeed |
| 25 (fix 2, load errors) | Change `LoadErrorState` / `errorDetail` | A new `loadErrorDetail(err, thing)` for every load error (toasts keep `errorDetail`); the gap page keeps its own 404 page ("This gap analysis no longer exists"), already true | The words say why it failed; only an unreachable app says "running" |
| 25 (fix 5, keys) | Map the Save job and Test sentences in the web app | The server writes "<Provider> refused your API key. Check it in Settings › AI & models." itself (`llm._no_answer`; Gemini's 400 API_KEY_INVALID counted as refused; "Your AI server" with a custom endpoint). `MISSING_KEY`/`REFUSED_KEY` are exported, one pattern each, covering the server's current and older refused-key sentences (coordinator: the Companion mirrors them). The shape check is skipped with a custom AI server, whose key has its own shape | One sentence for one failure on every surface |
| 25 (fix 8, words) | "today's application count"; "Linked to a connected agent's proposal"; "Add 14 things…"; "Refresh it" | "Couldn't load Applications per day. Try again" (the window is 24 hours, never "today", and that is the line's own name); "Linked to a proposal in Agent inbox" (a web Queue files the user's own proposal, so "an agent's" was false too); "things", since "item" is the glossary's word for a career history record; the text copy says it updates itself (every read rebuilds it) and keeps a Refresh button. The job market's OPT signal left the server (the tile shows it, with STEM OPT explained); the top location and skill are re-worded in the page (`signalTitle`), since the server keeps the stored name by design. "Let the Companion …": the convention now says a label that reads as a sentence takes "the" (planner decision 20, Q8, refined) | Plain words, one per thing, each true |
| 25 (fix 8, "Offer accepted") | Application status "Accepted" → "Offer accepted" | Not changed here: `test_extension_panel_track.py::test_the_panels_status_words_are_the_web_apps` pins the Companion's `STATUS_LABELS` (extension/panel/panel.js) equal to `status-chip.tsx`, and `extension/**` belongs to the Companion lane. Handed to the coordinator to change both copies in one commit at the Companion merge | One word per thing: a web-only rename would split the vocabulary and fail the parity pin |
| 25 (fix 8, backend down) | One page-level message when the backend is down | Not built: it needs a shared mechanism `LoadErrorState` does not have; logged as SYSTEM.md §11 item 38 | Scope: no new mechanism in a fix batch |
| 25 (fix 3, dates) | Say so on the gap page and the Score tab | The gap page's Format card already named "Jul 2022"; the Score tab gains the note, and a skills-list skill found in undated entries says why they don't count. The parser is unchanged (§11 item 37) | Honesty survives; scores move only with calibration |

## Gate results

| When | Gate | Result |
|---|---|---|
| Baseline (`61a4a15f`) | full backend / node / tsc / lint / duplication / hotspots / SYSTEM.md | 4969 passed, 2 skipped / 166 / clean / 0 errors, 5 warnings / 437 lines, 36 clones / 423 / 1000/1000 |
| Wave 1 merged (`508cd731`) + integration fixes (`1f039066`, `49dc41dc`, `92c3ec9a`) | full reviews per lane + integrated browser pass | lanes 1–4 each code-reviewed (mutation checks), fixed and browser-verified; integrated pass fixed 9 findings (incl. a double Queue creating two accepted proposals: single-flight + a SQLite write lock); pytest 5149 passed, 3 skipped; node 200; tsc/lint clean; build OK; dup 437/36; hotspots 424; check_system_md 1000/1000 |
| Wave 2 merged (`3cbeb06b`, `e1079386`) + follow-up (`9e2222f3`) | lanes 5–6 reviewed, fixed and merged; full backend / node / tsc / lint / build / duplication / hotspots | pytest 5256 passed, 3 skipped; node 227; tsc/lint clean; build OK; dup 437/36; hotspots 424 |
| Task 16 (`30c770bd`) | ratchets (19 mutations, each killed by its named test) + full gates | pytest 5317 passed, 3 skipped; `test_frontend_*.py` 950; node 230; ruff clean; tsc clean; lint 0 errors, 2 warnings; build OK; dup 437/36 and hotspots 424 on a clean `git archive HEAD`; slop check frontend, backend, extension OK; check_system_md 1000/1000. Pending: words 438 in 99 files, raw errors 159 in 70, placeholders 64 |
| Waves 1+2 integrated browser pass (`9e2222f3`) | cross-feature flows, 1280/768/375, light + dark | no merge regressions; 3 items handed to lane 9 (card "accept" wording, Autofill Save and New chat double-click, both pre-existing) |
| Wave 3 merged: lane 9 `4c15e5df`, lane 7 `4c3b83e6`, lane 8 `70402ca0`, lane 10 `7405e41d`; merge-time fixes `c1697a8e` | full backend / ruff / tsc / lint / node / build / slop on a clean `git archive HEAD` / SYSTEM.md; ratchets without pending blocks | pytest 5627 passed, 3 skipped; `test_frontend_*.py` 1190; ruff clean; tsc clean; lint 0 errors, 2 warnings; node 270; build OK (run last); frontend dup 378 lines / 33 clones (≤ 437/36), backend hotspots 424 (≤ 424), backend dup 404/44, extension 0 hotspots, 0 clones, 7 allowlisted; `slop_scan.py check` frontend, backend, extension OK; `check_system_md.py` 1000/1000. Vocabulary violations 0 in every file, raw error sites only the 3 allowlisted, no example placeholder; no `_PENDING*` name left in the three ratchets. New pins each seen failing first or mutation-killed (M1–M7 plus the red runs) |
| Tasks 20–23 | merged in wave 3 (row above): lane 8 `70402ca0` (Tasks 19–20), lane 9 `4c15e5df` (Task 21), lane 10 `7405e41d` (Tasks 22–23) | done |
| Task 24 docs sweep (`a55fbdde`) | `check_system_md.py`; `test_frontend_*.py` + `test_extension_manifest.py`; full backend | done: SYSTEM.md 968/1000, 0 warnings (§11 items 5, 29–32 narrowed, 34–36 added; §12 +5 lessons; §13 `postgres-to-sqlite` row); 1219 passed; `pytest tests/ mcp_server/tests/ -q` 5627 passed, 3 skipped |
| Task 25 final fix batch (`f1bc6fbe`, `3e8e5aca`, `810277af`) | full backend / `test_frontend_*.py` / ruff / tsc / lint / node / slop on a clean `git archive HEAD` / SYSTEM.md / build (last); browser on a throwaway stack (8895/3295, no LLM key; a made-up key reached OpenAI for the real 401) | pytest 5668 passed, 3 skipped; `test_frontend_*.py` 1224; ruff clean; tsc clean; lint 0 errors, 2 warnings; node 283; frontend dup 392 lines / 34 clones (≤ 437/36), backend hotspots 424 (≤ 424, eight pins split under cc 10), `slop_scan.py check` frontend, backend, extension OK; `check_system_md.py` 973/1000, 0 warnings; build OK. 43 mutations, each killed by its named pin. Browser: Keep it on the inbox row (refused unlinked: the fix sentence, focus kept; linked: back to To review, focus to the next row, never `<body>`) and on the job page (links the job's application); 404/422/500/502/network load errors; refused key on Test, Save job and the Assistant; wrong key shape refused before sending; Score tab and gap page date notes; Analytics and Job market words; Mark as OK / Undo focus traces with no `<body>` frame, light and dark; 375 dark and 768 with no side scroll |
| Task 25 final (`582c6dc9`) | full gates | pytest 5705 passed, 3 skipped; ruff clean; node 283; tsc clean; lint 0 errors, 2 warnings; build OK; slop OK on frontend (dup 392/34, from 437/36), backend (hotspots 424), extension (0 clones, 7 allowlisted); check_system_md 973/1000 |
| Task 25 browser | regression sweep + first-read pass | regression sweep (~1,900 checks, 1280/768/375, light and dark, real Back/Forward, no-TeX run): no regressions from this plan; the one plan-era leftover (Mark as OK focus flicker) fixed. First-read pass by a reader who never saw the audits, web app and Companion: most screens clear or mostly clear; the confusing ones (Needs-you answers, not-found and 500 fallbacks, unreadable dates vs skill gaps, Analytics skill-gap labels, key errors, a dead Companion button) fixed in the final batch |
| `main` merged (`666ec2af`: v0.4.0, v0.5.0, Postgres removal, next 16.3.6) | full gates on the merged tree | pytest 5659 passed, 1 skipped (was 5705/3: main deleted 46 legacy-Postgres tests incl. 2 of the skips, and the merge dropped this branch's 2 importer/legacy-chain `proposed_by` tests; the SQLite backfill and round-trip tests stay); `alembic heads` one, `9a5744f9b9d9` on baseline `871d0425b64c`; ruff clean; `npm ci` (next 16.3.6); tsc clean; lint 0 errors, 2 warnings; node 283; slop on a clean `git archive HEAD`: frontend dup 392/34 (≤ 437/36), backend hotspots 421 (≤ 424, main's deletions), extension 0 clones; `check` frontend, backend, extension OK (backend `.slop-baseline.json` still names `app.tools.migrate_from_postgres` and `**/legacy_postgres/**`: stale, not edited); check_system_md 964/1000; build OK (last) |

## Goal critique (Task 25)

Against the Goal Card, not the plan. **Verdict: approve.**

- **A new user understands every screen on first read.** A fresh reader rated most screens clear; the ones that confused were fixed in the final batch and re-verified. Residual: the template editor's "Short name" (a slug a user must type) and template names that name typesetting engines (§11); a page-level message when the backend is down instead of one per card (§11 item 38).
- **One plain word per thing, on every surface.** The glossary is single and pinned by three ratchets (banned words, raw error text, example placeholders) with no pending exemptions left; the Companion and the server's messages follow it, and web and Companion status words and key-error rules are pinned equal. Residual: words the ratchet has no rule for yet (listed in the glossary), and the owner's naming calls (§11 item 36: Applications vs Jobs and the "All" segment, Gender options, "restrictive covenant", the model price figures).
- **The Agent inbox is separate and names who proposed each job.** Done: its own page and vocabulary, the funnel moved to Analytics, Cap today, Needs-you count, "Proposed by …" / "Queued by you" on every surface, an honest Connected agents card, and a way to answer Needs-you proposals. Along the way: a double Queue that created two proposals was fixed at both ends (single-flight and a write lock), and diversity answers now reach the AI only under standing consent (one gate, fails closed).
- **Long lists keep their headers and say when they are cut off.** Toolbars stick at every width; table headers stick while the table fits (by design, B O1); 500-row caps say so. Residual: accepting more resume date formats needs ATS recalibration (§11 item 37).
- **Settings and Profile in tabs with one spacing rhythm.** Done, with deep links, the leave guard across tabs (and a Back/Forward regression the plan itself introduced, caught and fixed), status in the card header, and the Models split.
- **Everything from the last plan still holds.** The regression sweep re-ran the previous plan's checks: no typed text lost, focus never on `<body>` except after an in-app navigation (pre-existing, §11 item 29), one request per click, AA contrast (dark outline-button focus rings fixed to ≥3:1).
- **Process note.** Appendix D's wording was wrong in a few dozen places (each logged as a plan defect in the lane docs); accuracy-first reviews caught them. Next time, a copy plan should verify each proposed string against the code before handing it to executors.

WebKit/Safari and screen readers were not run (Chromium and its accessibility tree only).

